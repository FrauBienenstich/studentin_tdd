%h4 Student
%table{border: "1"}
  %thead
    %tr
      %th ID
      %th First Name
      %th Last Name
      %th Courses
  %tbody
    -@student.each do |student|
      %tr
        %td
          = student.id
        %td
          = student.first_name
        %td
          = student.last_name
        %td &mdash;
%a{href:'/'} Back