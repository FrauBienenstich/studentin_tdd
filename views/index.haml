%h4 Students
%table{border: "1"}
  %thead
    %tr
      %th ID
      %th First Name
      %th Last Name
      %th Courses
  %tbody
    - @students.each do |student|
      %tr
        %td
          = student.id
        %td
          = student.first_name
        %td
          = student.last_name
        %td &mdash;


%h4 Courses
%table{border: "1"}
  %thead
    %tr
      %th ID
      %th Title
      %th Students
  %tbody
    - @courses.each do |course|
      %tr
        %td
          = course.id
        %td
          = course.title
        %td &mdash;